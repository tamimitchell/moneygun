<% content_for :title, t(".title") %>

<% content_for :head do %>
  <%= turbo_refreshes_with method: :morph, scroll: :preserve %>
<% end %>

<%= turbo_stream_from "notifications_index_list_#{current_user.id}" %>

<%= render SectionComponent.new(title: t(".title")) do |component| %>
  <%= turbo_frame_tag :notifications, target: "_top" do %>
    <% if @notifications.any? %>
      <div class="table-responsive">
        <table>
          <tbody id="notifications">
            <%= render partial: "users/notifications/notification", collection: @notifications %>
          </tbody>
        </table>
      </div>
      <%= render "shared/pagination", pagy: @pagy %>
    <% else %>
      <%= render EmptyStateComponent.new(icon: "svg/bell.svg") %>
    <% end %>
  <% end %>
<% end %>
