<% content_for :title, t("organizations.projects.edit.title") %>

<%= modal title: t("organizations.projects.edit.title") do %>
  <%= render SectionComponent.new do |component| %>
    <%= render "form", project: @project %>
  <% end %>
<% end %>
