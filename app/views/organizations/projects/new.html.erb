<% content_for :title, t("organizations.projects.new.title") %>

<%= modal title: t("organizations.projects.new.title") do %>
  <%= render SectionComponent.new do |component| %>
    <%= render "form", project: @project %>
  <% end %>
<% end %>
