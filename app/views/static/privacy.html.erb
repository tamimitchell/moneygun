<% content_for :title, t(".title") %>
<% content_for :description, t(".description", site_name: Rails.application.config_for(:settings).dig(:site, :name)) %>

<%= modal do %>
  <%= render SectionComponent.new(full_width: false) do |component| %>
    <%= markdown_to_html(Rails.root.join("app/views/static/privacy.md").read) %>
  <% end %>
<% end %>
