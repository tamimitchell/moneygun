<% content_for :title, t(".title") %>
<% content_for :description, t(".description", site_name: Rails.application.config_for(:settings).dig(:site, :name)) %>

<%= render "organizations/subscriptions/pricing" %>

<br>

<%= render "organizations/subscriptions/features" %>
